# Dashboard & visualisation
streamlit>=1.32
plotly>=5.20
pandas>=2.0
numpy>=1.24

# Database
sqlalchemy>=2.0

# ML (Phase 2 — model inference for dashboard)
scikit-learn>=1.4
shap>=0.45
joblib>=1.3

# Simulation (Phase 1/3 — requires SUMO 1.18+ installed separately)
# traci is bundled with SUMO — add $SUMO_HOME/tools to PYTHONPATH
